1、开源协议：是指 GNU General Public License（GPL）和其他类似的开源协议，这些协议规定了开源软件的使用、复制、修改和分发等方面的规定。其中，GPL 是最常见的开源协议之一，它要求任何基于 GPL 许可的软件都必须开源并保持开源。
2、GNU：是一个自由软件运动的组织，致力于推广自由软件的理念和开发自由软件。GNU项目创建了许多自由软件，其中最著名的是GNU操作系统。GNU操作系统的内核是GNU Hurd，但也常常与Linux内核结合使用，形成了GNU/Linux操作系统。
3、Halt：是一个用于停止程序执行的指令或函数。在计算机编程中，当程序执行到halt指令时，程序将立即停止执行并退出。
4、创建/root/docx.txt文件总结命令
    touch /root/docx.txt
    vi docx.txt
        ls             查看目录
        ls -lh         
        pwd            查看当前工作路径    
        cd             切换路径
        cat            查看文件内容（小）
        tac            从最后一行显示内容
        less           查看文件内容（大）
        more
        mkdir          创建目录
        touch          创建文件
        vi             打开文件
        ip a           ip地址
        halt           关机
        shutdown      
        :wq!
5、Atime ：访问时间
   Mtime : 数据修改时间
   Ctime : 状态修改时间
6、补全 （Tab）
   命令的补全：
      命令的解析：当shell接收到一条命令之后，按照空格对命令进行切割，将命令的最左侧切割的字符串当作命令的去执行，判断命令是否为内部命令，若不是内部命令，则依赖shell的环境变量PATH中定义的路径进行命令的查找，自左向右依次查找，将一个找的命令，当作当前执行的命令去执行。
      路径的补全：
          目录、文件
7、快捷键：
    clear     清屏 
    Ctrl+l    
    Ctrl+c    终止命令
    Ctrl+d    退出
    Ctrl+a    光标移到命令行的最前端
    Ctrl+e    光标移到命令行的最后端
    Ctrl+u    删除光标前所有字符
    Ctrl+k    删除光标后所有字符
8、命令历史
    history  
9、别名
    alias NAME="command"
10、重定向
    >    标准输出重定向
    >>   标准输出追加
    2>   错误输出重定向
    2>>  错误输出追加
    &>   混合输出重定向
    2>&1  
11、管道: 将上一个命令的执行结果当作下一个命令的输入
    tee :在数据流的处理过程中将某段信息保存下来，使其既能输出到屏幕又能保存到某一个文件中。
12、vim 三种模式切换
    1、默认命令模式
    2、a o i 切换到输入模式
    3、":" 切换到末行模式  



